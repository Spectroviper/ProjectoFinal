mutation LockUnlockChallengeAchievement($userId: ID, $userChallengeAchievementId: ID) {
  lockUnlockChallengeAchievement(UserId: $userId, UserChallengeAchievementId: $userChallengeAchievementId) {
    message
    successfull
  }
}